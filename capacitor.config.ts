import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saricden.relicReclaimation',
  appName: 'Relic Reclaim',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
